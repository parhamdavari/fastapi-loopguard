[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "fastapi-loopguard"
version = "0.6.1"
description = "Detect event-loop blocking in FastAPI/Starlette with per-request attribution"
readme = "README.md"
license = "MIT"
requires-python = ">=3.12"
authors = [
    { name = "Parham" }
]
keywords = [
    "fastapi",
    "starlette",
    "asyncio",
    "middleware",
    "monitoring",
    "performance",
    "blocking",
    "event-loop",
]
classifiers = [
    "Development Status :: 4 - Beta",
    "Framework :: AsyncIO",
    "Framework :: FastAPI",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Programming Language :: Python :: 3.14",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Topic :: System :: Monitoring",
    "Typing :: Typed",
]
dependencies = [
    "starlette>=0.37.0,<2.0",
]

[project.optional-dependencies]
# Every extra must make an observable difference once installed:
#   prometheus -> metrics.py wires up LoopGuardMetrics
#   dev        -> the test, lint and type-check toolchain
#   stress     -> examples/ needs a server and a load generator
#   all        -> alias for every optional runtime feature
prometheus = ["prometheus-client>=0.19.0"]
all = [
    "prometheus-client>=0.19.0",
]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "httpx>=0.27.0",
    "fastapi>=0.110.0",
    # Without this, tests/test_metrics.py importorskip-skips silently and
    # metrics.py sits at 0% coverage in CI
    "prometheus-client>=0.19.0",
    "mypy>=1.8.0",
    "ruff>=0.3.0",
    "coverage>=7.4.0",
    "jsonschema>=4.21.0",
]
stress = [
    "locust>=2.20.0",
    "uvicorn>=0.27.0",
]

[project.urls]
Homepage = "https://github.com/parhamdavari/fastapi-loopguard"
Documentation = "https://github.com/parhamdavari/fastapi-loopguard#readme"
Repository = "https://github.com/parhamdavari/fastapi-loopguard"
Issues = "https://github.com/parhamdavari/fastapi-loopguard/issues"

[project.entry-points.pytest11]
loopguard = "fastapi_loopguard.pytest_plugin"

[project.scripts]
# cli.py is stdlib-only and must never import pytest_plugin: the console
# script has to work in an install with no dev extras.
loopguard = "fastapi_loopguard.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/fastapi_loopguard"]

[tool.hatch.build.targets.sdist]
exclude = [
    "/.claude",
    "/stress_results*.csv",
    "/.coverage",
    "/.pytest_cache",
    "/.mypy_cache",
    "/.ruff_cache",
]

[tool.pytest.ini_options]
asyncio_mode = "auto"
asyncio_default_fixture_loop_scope = "function"
testpaths = ["tests"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_return_any = true
warn_unused_configs = true

[[tool.mypy.overrides]]
module = "prometheus_client.*"
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = "locust.*"
ignore_missing_imports = true

[tool.ruff]
# target-version is intentionally unset: ruff derives it from
# project.requires-python, so the UP rewrites can never target a version the
# package does not claim to support.
line-length = 88
src = ["src", "tests"]

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W", "UP", "B", "C4", "SIM", "ANN"]
ignore = ["ANN401"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.coverage.run]
source = ["src/fastapi_loopguard"]
branch = true

[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
